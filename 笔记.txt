在编写时间设置程序时，出现了节点无法进入的问题，经排查，是heap内存溢出的原因。在创造每个节点时，选用malloc动态分配内存，而在每个节点中，为了容纳子节点，又创建了maxchildren个二维指针，结构体体积过大，从而导致heap内存不够。同时，也有heap内存过小的原因（512B，startup文件查找）。在动态分配内存时，尽量减少无用内存占用的产生，或者使用静态分配内存，（例子：PageNode page_3_1;）这样初始化的全局变量分配在.data中，未初始化的全局变量在.bss中，局部变量在.stack中。